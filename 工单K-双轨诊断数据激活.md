# 工单K：双轨诊断数据激活（种子数据 + 数据看板联动）

## 状态
待执行

## 优先级
P0 — 核心业务闭环断裂

## 验收报告引用
- R1: 双轨诊断无数据，页面显示"暂无诊断数据"
- R3: 数据看板核心指标全空（平均学习进度/质检分/业务达标率/热力图/阶段分布）
- G4: 培训老师工作台全零数据

## 根因
数据库 `business_data` 表无任何SCRM业务数据，导致：
1. `/api/diagnosis` 查询不到指标 → 诊断页空
2. `/api/dashboard` 聚合查询返回空 → 数据看板全空
3. 培训老师工作台依赖质检/课程数据 → 全零

## 实施方案

### Step 1: 创建种子数据脚本
为3个trainee（zhangxh/lidw/wangml）生成合理的业务数据：
- `business_data` 表插入6项结果指标（加V率/面诊率/接诊率/签收率/用药率/挂号率）
- `qc_records` 表插入质检记录（4维度评分）
- `quadrant_snapshots` 表插入历史快照

### Step 2: 确保诊断API能消费种子数据
- 验证 `/api/diagnosis` 返回正确的逐项对标结果
- 验证四象限分类与散点图一致

### Step 3: 验证数据看板联动
- 确认 dashboard API 聚合数据正确
- 确认热力图、阶段分布柱状图有数据渲染

### Step 4: 验证培训老师工作台
- 确认待审核质检/今日课程等数据正确

## 涉及文件
- `src/app/api/diagnosis/route.ts` — 诊断API（确认无bug）
- `src/app/api/dashboard/route.ts` — 数据看板API
- 种子数据SQL脚本（新建）

## 验收标准
- [ ] 双轨诊断页显示3个trainee的诊断数据（逐项对标+四象限）
- [ ] 数据看板显示核心指标（非零）
- [ ] 热力图有解锁状态
- [ ] 阶段分布柱状图有数据
- [ ] 培训老师工作台有数据
- [ ] 散点图与分类统计一致

## 角色分配
- 业务：国民萌叔
- 代码：Agent
- 测试：Agent
- 审查：国民萌叔
